import 'package:flutter/material.dart';

Widget addHorizontalSpace(double space) => SizedBox(
      width: space,
    );
Widget addVerticalSpace(double space) => SizedBox(
      height: space,
    );
